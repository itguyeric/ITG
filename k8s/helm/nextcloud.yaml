apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: itg-nextcloud
  namespace: itguyeric
spec:
  releaseName: itg-nextcloud
  chart:
    repository: https://nextcloud.github.io/helm/
    name: nextcloud
    version: 2.5.1
  values:
    service:
      type: LoadBalancer
