---
#- name: enable codeready-builder
#  when: ansible_facts['distribution'] == "RedHat"
#  rhsm_repository:
#    name: "codeready-builder-for-rhel-{{ ansible_distribution_major_version }}-{{ ansible_architecture }}-rpms"
#    state: enabled    

#- name: import epel gpg key
#  when: (ansible_facts['distribution'] == "RedHat") or
#        (ansible_facts['distribution'] == "CentOS")
#  rpm_key:
#    key: "https://dl.fedoraproject.org/pub/epel/RPM-GPG-KEY-EPEL-{{ ansible_distribution_major_version }}"
#    state: present
#
#- name: add epel repository
#  when: (ansible_facts['distribution'] == "RedHat") or
#        (ansible_facts['distribution'] == "CentOS")
#  package:
#    name: "https://dl.fedoraproject.org/pub/epel/epel-release-latest-{{ ansible_distribution_major_version }}.noarch.rpm"
#    state: latest
#
- name: install baseline utilities
  package:
    name: "{{ item }}"
    state: latest
# TODO: Move list to vars file
  with_items:
    - bash-completion
    - curl
    - dnf
    #- htop
    #- kernel-devel
    #- speedtest-cli
    - wget

# TODO: Manual install for CentOS 8 - https://www.speedtest.net/apps/cli

- name: install fedora-specific packages
  when: ansible_facts['distribution'] == "Fedora"
  package:
    name: dnf-plugin-system-upgrade
    state: latest
